# yaml-language-server: $schema=https://app.stainless.com/config.schema.json

# The main edition for the config, see the [docs] for more information.
#
# [docs]: https://www.stainless.com/docs/reference/editions
edition: 2026-01-30

organization:
  # Name of your organization or company, used to determine the name of the client
  # and headings.
  name: cozmoai
  # Link to your API documentation.
  docs: 'https://docs.cozmox.ai/'
  # Contact email for bug reports, questions, and support requests.
  contact: ''

# `targets` define the output targets and their customization options, such as
# whether to emit the Node SDK and what its package name should be.
targets:
  python:
    # The edition for this target, see the [docs] for more information.
    #
    # [docs]: https://www.stainless.com/docs/reference/editions
    edition: python.2025-11-20
    package_name: cozmoai
    production_repo: null
    publish:
      pypi: false
  openapi: {}
  typescript:
    edition: typescript.2025-10-10
    package_name: cozmoai
    production_repo: null
    publish:
      npm: false
  go:
    edition: go.2026-2-3
    package_name: cozmoai
    production_repo: null

# `environments` are a map of the name of the environment (e.g. "sandbox",
# "production") to the corresponding url to use.
environments:
  production: https://nova.prod.czmx.in/api

# `resources` define the structure and organization for your API, such as how
# methods and models are grouped together and accessed. See the [configuration
# guide] for more information.
#
# [configuration guide]: https://www.stainless.com/docs/guides/configure#resources
resources:
  agents:
    models:
      agent_response: '#/components/schemas/agents.AgentResponse'
      background_sound_config: '#/components/schemas/agents.BackgroundSoundConfig'
      extra_config: '#/components/schemas/agents.ExtraConfig'
      goodbye_config: '#/components/schemas/agents.GoodbyeConfig'
      greeting_config: '#/components/schemas/agents.GreetingConfig'
      llm_config: '#/components/schemas/agents.LLMConfig'
      room_duration_config: '#/components/schemas/agents.RoomDurationConfig'
      transcriber_config: '#/components/schemas/agents.TranscriberConfig'
      vad_config: '#/components/schemas/agents.VadConfig'
      voice_config: '#/components/schemas/agents.VoiceConfig'
    methods:
      list: get /org/{org_id}/agents
      create: post /org/{org_id}/agents
      delete: delete /org/{org_id}/agents/{agent_id}
      retrieve: get /org/{org_id}/agents/{agent_id}
      update: patch /org/{org_id}/agents/{agent_id}
  calls:
    methods:
      list: get /org/{org_id}/calls
      retrieve: get /org/{org_id}/calls/{call_id}
  workflows:
    methods:
      list: get /org/{org_id}/workflows
      retrieve: get /org/{org_id}/workflows/{workflow_id}
  voices:
    methods:
      list: get /org/{org_id}/voices

settings:
  # All generated integration tests that hit the prism mock http server are marked
  # as skipped. Removing this setting or setting it to false enables tests, but
  # doing so may result in test failures due to bugs in the test server.
  #
  # [prism mock http server]: https://stoplight.io/open-source/prism
  disable_mock_tests: true
  license: Apache-2.0

# `client_settings` define settings for the API client, such as extra constructor
# arguments (used for authentication), retry behavior, idempotency, etc.
client_settings:
  opts:
    api_key:
      type: string
      nullable: false
      auth:
        security_scheme: BearerAuth
      read_env: COZMOAI_API_KEY
    org_id:
      type: string
      nullable: false
      read_env: COZMO_ORG_ID

security:
  - BearerAuth: []

# `readme` is used to configure the code snippets that will be rendered in the
# README.md of various SDKs. In particular, you can change the `headline`
# snippet's endpoint and the arguments to call it with.
readme:
  example_requests:
    default:
      type: request
      endpoint: get /org/{org_id}/agents
      params: {}
    headline:
      type: request
      endpoint: get /org/{org_id}/agents
      params: {}
